
<div class="container text-center">
  <h1 class="display-1">Lists</h1>

  <% @lists.each do |list| %>
    <div class="fs-2">
      <%= list.id %> - <%= link_to list.name, list_path(list.id), class: "text-decoration-none" %>
    </div>
  <% end %>
</div>
